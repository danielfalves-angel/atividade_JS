function vogalConso() {
    let letra = (prompt("digite uma letra:"));
    alert (letra);
    if (letra = a && e && i && o && u) {
        alert("essa letra é vogal");
    } else {
        alert("essa letra é consoante");
    }
}